# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: Canonical Ltd

name: Reuse
on:
    push:
    pull_request:
jobs:
    redocly:
        runs-on: ubuntu-latest
        name: Lint and build
        container:
            image: fsfe/reuse
            volumes:
                - .:/spec
        steps:
            - name: Checkout
              uses: actions/checkout@v5
            - name: Create placeholder redoc_static.html
              run: touch redoc-static.html
            - name: Check Licensing/Copyright Compliance
              run: reuse lint