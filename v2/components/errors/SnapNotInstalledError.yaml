# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: © 2025 Canonical Ltd (https://https://github.com/canonical/snapd)

type: object
properties:
  kind:
    type: string
    enum:
      - 'snap-not-installed'  # TODO Need follow up PR focusing only on errors
  message:
    type: string
    example: 'snap \"moon-buggy\" is not installed'
  value:
    type: string
    example: 'moon-buggy'
