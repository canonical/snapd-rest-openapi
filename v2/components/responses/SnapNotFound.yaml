# SPDX-License-Identifier: GPL-3.0
# SPDX-FileCopyrightText: Canonical Ltd

description: 
  - The snap provided in 'value' was not found.
content:
  application/json:
    schema:
      type: object
      properties:
        status-code:
          type: integer
          enum:
            - 404
        status:
          type: string
          enum: 
            - Not Found
        type:
          type: string
          enum:
            - error
        result:
          $ref: '../errors/SnapNotInstalledError.yaml'

